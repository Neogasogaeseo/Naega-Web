import { COLOR } from '@styles/common/color';
import styled from 'styled-components';

export const StExpandListButton = styled.div`
  display: flex;
  gap: 6px;
  justify-content: center;
  align-items: center;
  color: ${COLOR.GRAY_4};
  margin: 18px 0;
  cursor: pointer;
`;
