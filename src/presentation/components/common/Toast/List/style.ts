import styled from 'styled-components';

export const StToastList = styled.div`
  bottom: 0;
  left: 0;
  right: 0;
  position: fixed;
  z-index: 1000;
  width: calc(100% - 40px);
  max-width: 350px;
  margin: 0 auto;
`;
